RequireJS Optimizer + jQuery + jQuery.URL (i.e. "purl") issue
=============================================================

This project reproduces a bug in one or many of the above libraries.

Reproduction requires NodeJS to be installed. Here is the command to build:

    node r-2.2.1.js -o build.js
